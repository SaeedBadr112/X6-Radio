. تثبيت أداة الأصول الرسمية (مرة واحدة فقط)
في موجه الأوامر (داخل مجلد المشروع)، قم بتشغيل هذا الأمر لتثبيت الحزمة المسؤولة عن توليد الصور:

bash
npm install @capacitor/assets --save-dev
2. تشغيل أمر التوليد
بعد التثبيت، قم بتشغيل الأمر التالي مباشرة:

bash
npx capacitor-assets generate
